# Инструкция для AI по работе с декларациями в Альте

## 0. Этапы работы

Промпты поздних этапов не требуются на текущем этапе. Этапы зависимы друг от друга только в направлении от раннего 
к позднему. 

### Этап 1.

AI разбирает первичные документы по поставке, извлекает и сверяет факты. При необходимости AI переходит в режим диалога
с оператором для сбора недостающих фактов и разрешения конфликтов.
На основе собранных данных и шаблонов формализации AI формирует набор формализованных документов для:
- импорта формализованных первичных документов в Альту
- файл формализованных исходных данных для этапа 2
- отчет по этапу 1 для оператора
- возможно, файл, содержащий сведения, полученные от оператора (может пригодиться для повторных прогонов)

Исходные данные лежат в:

- `alta\source` первичные документы
- `alta\stable_source` документы, общие для кейсов. Например, данные декларанта.

Шаблоны и правила лежат в `alta\prompt\stage_1`:

- `primary_schema.md` извлеченные из первички факты, а также пояснения оператора. 
- `formalized_docs_map.md` мапирование полей документов из `primary_schema.md` в xml-теги импортируемых формализованных
  документов.
- `review_1_schema.md` шаблон отчета по этапу 1

Результаты этапа 1 лежат в папке `alta\stage_1_result`:

- `primary.md` факты поставки, собранные из первички и готовые для этапа 2 и генерации импортируемых формализованных
  документов.
- `formilized_docs` каталог, в который помещаются xml файлы формализованных документов, предназначенных для импорта в Альту
- `review_1.md` - отчет по этапу 1
- `alta\source\operator\operator_provided_data.md` - опционально, данные, полученные от оператора (из диалога с оператором, 
  если у AI возникли вопросы)

### Этап 2

Исходные данные лежат в папке `alta\stage_1_result`:

- `primary.md` 

Шаблоны и правила лежат в `alta\prompt\stage_2`:

- `trans_schema.md` - слой подготовки полей dt.xml из первичных данных `primary.md`
- `review_2_schema.md` - схема отчета по этапу 2

Результаты этапа 2 лежат в папке `alta\stage_2_result`:

- `transs.md` основной слой подтвержденных и производных данных
- `review_2.md` отчет по этапу 2

### Этап 3

Шаблоны и правила лежат в `alta\prompt\stage_3`:

- `trans_map.md` - правила мапирования фактов из trans.md в теги xml или поля UI.
- `dt_xml_schema.md` промежуточный промпт для генерации `dt_import.xml`.
- `dt_ui_schema.md` опциональный промпт для генерации инструкции ручного ввода.

Результаты этапа 3 лежат в папке `alta\stage_3_result`:

- `xml_xml.md` база для формирования `dt_import.xml`
- `dt.xml`, пригодный для импорта в Альту`ui_input.md` — опционально, карта ввода ДТ в Альту. Следует формировать в кодировке `cp1251`.
- `dt_ui.md` - опциональный промпт для генерации инструкции ручного ввода.

### Этап 4

- Ручной или автоматический импорт `formalized_docs.xml` в Альту.
- Ручной или автоматический импорт `dt.xml` в Альту.

После завершения любого этапа возможно и желательно очистить историю чата.

---

## 1. Базовые правила

### 1.1. Никакой отсебятины

Любое значение, попадающее в `primary.md`, должно быть:

- извлечено из первичных документов;
- либо подтверждено оператором;
- либо корректно выведено из подтвержденных данных;
- либо явно помечено как эталонное наблюдение, не являющееся фактом новой поставки.
  Если данных не хватает — остановиться и обратиться к оператору либо явно зафиксировать пробел.

---

### 1.2. Несоответствия между документами считаются ошибками

Расхождения по:

- адресам;
- местам;
- весам;
- количеству;
- датам;
- суммам;
- валюте;
- моделям;
- обозначениям;
- условиям поставки;
- реквизитам сторон
  считаются конфликтом.

AI обязан:

1. назвать поле конфликта;
2. указать документы-источники;
3. указать расходящиеся значения;
4. остановить автоматическое продвижение по этому полю;
5. дождаться уточнения.

---

## Общение с оператором

### Один вопрос за раз

Если требуется вмешательство оператора, AI задает один короткий вопрос с указанием заблокированного поля.

---

## Примечание

- Хобот предоставляет команду перетаскивания файлов в поле ввода чата. Используй для pdf или файлов образов.

- каталог alta/alta_help содержит файлы помощи.

## Задание

- прочитай структуру каталога `alta`;
- загрузи шаблоны `alta\templates\facts_schema.md`, `alta\templates\review_schema.md`;
- выйди в режим диалога.