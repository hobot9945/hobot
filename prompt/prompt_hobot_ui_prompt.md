### Путь к этому файлу: 
prompt\prompt_hobot_prompt.md

# КОМАНДЫ ХОБОТА: справочник команд интерфейса пользователя

## Раздел: Управление окнами

### Команда: `get_window_list`
**Назначение:** получить список top-level окон Windows, которые видит агент (через `EnumWindows`), с опциональными
флагами включения “мусора”.

**Параметры:**
- параметры **опциональны**
- если параметры не заданы, вывод минимален - видимые окна с непустыми заголовками
- если параметры заданы:
    - `params`: массив строк (0..N)
    - порядок параметров **не важен**
    - поддержанные флаги:
        - `"include_invisible"` — включить невидимые окна (`IsWindowVisible=false`)
        - `"include_empty_title"` — включить окна с пустым заголовком

**Вывод:**
- Markdown-отчёт:
    - число окон
    - таблица: `#`, `x`, `y`, `width`, `height` ,`hwnd`, `title`
    - в конце `title` могут быть метки в скобках:
        - `(foreground)` — окно сейчас foreground
        - `(minimized)` — окно свернуто
        - `(invisible)` — окно невидимо (появляется только если включен `include_invisible`)

---

### Команда: `get_foreground_window_info`
**Назначение:** получить `HWND` и полный заголовок окна, которое в данный момент находится в фокусе (активно).

**Параметры:**
- параметров **нет** (`params` не указывать)

**Вывод:**
- Markdown-отчет, содержащий `HWND` (в hex-формате) и `title` активного окна.

---

### Команда: `find_window_info`
**Назначение:** найти окно по подстроке заголовка и вернуть его `HWND`, полный заголовок и геометрию.  
Команда **НЕ** фокусирует окно и **НЕ** делает скриншот.

**Параметры:**
- `params`: массив из **1 строки**
    - `params[0]`: `"<needle>"` — подстрока для поиска в заголовке окна.

**Вывод:**
- Markdown-отчет с `HWND`, полным заголовком (`title`), позицией (`pos: [x, y]`) и размером (`size: WxH`) найденного окна.
  Если окон несколько, будет выбрано первое найденное.

---

### Команда: `focus_window_by_hwnd`
**Назначение:** сфокусировать (перевести в foreground) окно по его дескриптору `HWND`.

**Параметры:**
- `params`: массив из **1 строки**
    - `params[0]`: `"<hwnd>"` — дескриптор окна:
        - десятичное число (`"123456"`)
        - или hex в формате `"0x..."` (`"0x1A2B3C"`)

**Вывод:**
- Markdown-отчет с `hwnd_in`, нормализованным `hwnd`, заголовком `title`, позицией `pos: [x, y]` и
  размером `size: WxH`.

**Примечания:**
- Фокусировка выполняется с ретраями внутри `library::window::focus_window()`.

---

### Команда: `move_mouse_in_window_by_hwnd`
**Назначение:** сфокусировать (перевести в foreground) окно по его дескриптору `HWND` и переместить указатель мыши
относительно левого верхнего угла окна.

**Параметры:**
- `params`: массив из **3 строк**
    - `params[0]`: `"<hwnd>"` — дескриптор окна:
        - десятичное число (`"123456"`)
        - или hex в формате `"0x..."` (`"0x1A2B3C"`)
    - `params[2]`: `"<x>"` — координата X
    - `params[3]`: `"<y>"` — координата Y

**Вывод и примечания:**
- повторяют `focus_window_by_hwnd`.

---

### Команда: `focus_window_by_title`
**Назначение:** найти окно по подстроке заголовка и сфокусировать его.

**Параметры:**
- `params`: массив из **1 строки**
    - `params[0]`: `"<needle>"` — подстрока для поиска в заголовке окна.

**Вывод:**
- Markdown-отчет с `needle`, `hwnd`, `title`, позицией `pos: [x, y]` и размером `size: WxH`.

**Примечания:**
- Команда использует `library::window::find_window_by_title_and_focus()` (поиск + фокусировка с ретраями).
- Если окон несколько, будет выбрано первое найденное (как в `find_window_info`).

---

### Команда: `move_mouse_in_window_by_title`
**Назначение:** найти окно по подстроке заголовка и сфокусировать его, переместить указатель мыши относительно левого
верхнего угла окна.

**Параметры:**
- `params`: массив из **1 строки**
    - `params[0]`: `"<needle>"` — подстрока для поиска в заголовке окна.
    - `params[2]`: `"<x>"` — координата X
    - `params[3]`: `"<y>"` — координата Y

**Вывод и примечания:**
- повторяют `focus_window_by_title`.

---

## Раздел: скриншоты

### Экономия трафика
Старайся заказывать скриншоты минимально необходимого размера, поскольку образы сильно расходуют трафик.

### Скриншоты и курсор мыши
Команды, возвращающие скриншоты (например: `capture_virtual_screen`, `capture_monitor`, `capture_region`,
`capture_window_by_title`, `capture_window_by_hwnd`)
могут:
- **наложить курсор мыши** на изображение (если он был видим в момент захвата);
- дополнительно вывести в отчёт строку вида:
    - `Курсор: (x, y)` — если hotspot курсора попадает в область изображения;
    - `Курсор: (x, y) [вне изображения]` — если hotspot вне области изображения;
    - `Курсор: скрыт` — если курсор был невидим в момент захвата.

Координаты `(x, y)` — это **hotspot курсора** относительно левого верхнего угла захваченной области/изображения.

---

### Координатная сетка для AI

Некоторые screenshot-команды поддерживают **опциональное наложение координатной сетки** поверх изображения.

### Назначение
Сетка предназначена **исключительно для навигации AI по скриншоту**:
- помогает привязывать элементы интерфейса к координатам;
- позволяет обсуждать области изображения в терминах `x/y`;

### Общие правила задания параметров сетки
Если команда поддерживает сетку, то после обязательных параметров команды можно передать:

1. `"<grid_step>"` — шаг сетки в пикселях:
    - целое число;
    - допустимо только значение `>= 5`;
    - если параметр не передан — сетка **не накладывается**.

2. `"<grid_color>"` — цвет линий сетки:
    - строка вида `"(r, g, b, a)"`;
    - `r`, `g`, `b` — целые значения `0..=255`;
    - `a` — вещественное число `0.0..=1.0`;
    - если цвет не передан, но шаг передан — используется цвет по умолчанию:
        - `"(255, 0, 0, 0.4)"`.

### Визуальное поведение сетки
При включении сетки:
- поверх изображения рисуются вертикальные и горизонтальные линии толщиной 1 пиксель;
- шаг по X и Y одинаковый;
- вокруг изображения автоматически добавляются поля для координатных подписей;
- подписи отражают **координаты в пикселях** относительно скриншота;

---

### Команда: `get_monitor_layout`
**Назначение:** вывести раскладку мониторов (логическая нумерация → физическая + геометрия).

**Параметры:**
- параметров **нет** (`params` не указывать)

**Вывод:**
- Markdown с количеством мониторов и таблицей:
    - `logical` — логический индекс монитора
    - `physical` — физический индекс `xcap::Monitor::all()`
    - `x`, `y`, `width`, `height` — геометрия в координатах виртуального рабочего стола

---

### Команда: `capture_virtual_screen`
**Назначение:** сделать скриншот всех мониторов, поместить в clipboard и вставить в поле ввода AI (Ctrl+V).

**Параметры:**
- параметров **нет** (`params` не указывать)
- **или**
- `params`: массив из **1 или 2 строк**
    - `params[0]`: `"<grid_step>"` — шаг сетки в пикселях (`>= 5`)
    - `params[1]`: `"<grid_color>"` — опциональный цвет сетки в формате `"(r, g, b, a)"`

**Примеры `params`:**
- `["100"]`
- `["100", "(255, 0, 0, 0.4)"]`

**Примечания:**
- См. раздел **“Скриншоты и курсор мыши (важно)”**.

---

### Команда: `capture_monitor`
**Назначение:** сделать скриншот одного монитора по логическому индексу, поместить в clipboard и вставить в поле ввода AI.

**Параметры:**
- `params`: массив из **1, 2 или 3 строк**
    - `params[0]`: `"<logical_index>"` — индекс монитора как строка числа (`"0"`, `"1"`, ...)
    - `params[1]`: `"<grid_step>"` — опциональный шаг сетки в пикселях (`>= 5`)
    - `params[2]`: `"<grid_color>"` — опциональный цвет сетки в формате `"(r, g, b, a)"`

**Примечания:**
- Если индекс вне диапазона, команда вернёт ошибку (например: доступно 2 монитора).
- См. раздел **“Скриншоты и курсор мыши (важно)”**.

---

### Команда: `capture_region`
**Назначение:** сделать скриншот прямоугольной области виртуального рабочего стола, поместить в clipboard и вставить в
поле ввода AI.

**Параметры:**
- `params`: массив из **4, 5 или 6 строк**
    - `params[0]`: `"<x>"` — координата X (может быть отрицательной в многомониторных системах)
    - `params[1]`: `"<y>"` — координата Y (может быть отрицательной)
    - `params[2]`: `"<width>"` — ширина области
    - `params[3]`: `"<height>"` — высота области
    - `params[4]`: `"<grid_step>"` — опциональный шаг сетки в пикселях (`>= 5`)
    - `params[5]`: `"<grid_color>"` — опциональный цвет сетки в формате `"(r, g, b, a)"`

**Примечания:**
- См. раздел **“Скриншоты и курсор мыши (важно)”**.

---

### Команда: `capture_mouse_vicinity`
**Назначение:** сделать скриншот прямоугольной области вокруг мыши, поместить в clipboard и вставить в
поле ввода AI.

**Параметры:**
- параметры **опциональны**
- допустимые форматы:
    - без параметров:
        - используется область `150x70`
    - `params`: массив из **2 строк**
        - `params[0]`: `"<width>"` — ширина области
        - `params[1]`: `"<height>"` — высота области
    - `params`: массив из **3 строк**
        - `params[0]`: `"<width>"`
        - `params[1]`: `"<height>"`
        - `params[2]`: `"<grid_step>"` — шаг сетки в пикселях (`>= 5`)
    - `params`: массив из **4 строк**
        - `params[0]`: `"<width>"`
        - `params[1]`: `"<height>"`
        - `params[2]`: `"<grid_step>"`
        - `params[3]`: `"<grid_color>"` — цвет сетки в формате `"(r, g, b, a)"`

**Примечания:**
- См. раздел **“Скриншоты и курсор мыши (важно)”**.

---

### Команда: `capture_window_by_title`
**Назначение:** найти окно по подстроке, сфокусировать его, сделать скриншот только этого окна, поместить в clipboard и
вставить в поле ввода AI.

**Параметры:**
- `params`: массив из **1, 2 или 3 строк**
    - `params[0]`: `"<needle>"` — подстрока для поиска окна
    - `params[1]`: `"<grid_step>"` — опциональный шаг сетки в пикселях (`>= 5`)
    - `params[2]`: `"<grid_color>"` — опциональный цвет сетки в формате `"(r, g, b, a)"`

**Вывод:**
- Markdown-отчет с сообщением о выполнении, полным заголовком, геометрией захваченного окна (ширина, высота, координаты)
  и информацией о курсоре мыши.

**Примечания:**
- Команда выполняет "best effort" фокусировку окна перед захватом.
- См. раздел **“Скриншоты и курсор мыши (важно)”**.

---

### Команда: `capture_window_by_hwnd`
**Назначение:** сделать скриншот окна по его дескриптору (`HWND`), поместить в clipboard и вставить в поле ввода AI.

**Параметры:**
- `params`: массив из **1, 2 или 3 строк**
    - `params[0]`: `"<hwnd>"` — дескриптор окна (десятичное число или hex в формате `"0x..."`)
    - `params[1]`: `"<grid_step>"` — опциональный шаг сетки в пикселях (`>= 5`)
    - `params[2]`: `"<grid_color>"` — опциональный цвет сетки в формате `"(r, g, b, a)"`

**Вывод:**
- Markdown-отчет с сообщением о выполнении, полным заголовком, геометрией захваченного окна (ширина, высота, координаты)
  и информацией о курсоре мыши.

**Примечания:**
- Команда выполняет "best effort" фокусировку окна перед захватом.
- См. раздел **“Скриншоты и курсор мыши (важно)”**.

---

## Раздел: Эмуляция клавиатуры и вставка текста

### Команда: `paste_text_into_window_by_title`
**Назначение:** вставить текст в окно, найденное по подстроке заголовка (needle), через `clipboard + Ctrl+V`
**с верификацией** (Ctrl+A/Ctrl+C).

**Параметры:**
- `params`: массив из **2 строк**
    - `params[0]`: `"<needle>"` — подстрока заголовка окна (поиск `contains`, регистр важен)
    - `params[1]`: `"<text>"` — текст для вставки

**Вывод:**
- Markdown-блок вида:
    - `needle='...'`
    - `hwnd=0x...`
    - `title='...'`
    - `pos: [x, y]`
    - `size: WxH`
    - `len=<N>`

**Примечания:**
- Команда **сама** фокусирует найденное окно (best effort).
- Команда считает вставку успешной только если смогла **подтвердить** наличие текста в текущем поле ввода через
  копирование (Ctrl+A → Ctrl+C).
- Поиск окна по needle ожидает **однозначное совпадение**: если найдено 0 или >1 окон — будет ошибка.
- Текст вставляется в **текущее поле ввода** внутри окна. Если в окне нет активного text-input (курсор не стоит в поле),
  верификация, скорее всего, упадёт.

**Побочные эффекты:**
- Временно перезаписывает системный буфер обмена.
- Пытается восстановить буфер обмена **только для текстового содержимого** (если до команды в буфере было
  изображение/файлы/HTML — восстановление “как было” не гарантируется).
- Генерирует события клавиатуры: `Ctrl+V`, `Ctrl+A`, `Ctrl+C`, стрелка вправо.

---

### Команда: `paste_text_into_window_by_hwnd`
**Назначение:** вставить текст в окно по `HWND` через `clipboard + Ctrl+V` **с верификацией** (Ctrl+A/Ctrl+C).

**Параметры:**
- `params`: массив из **2 строк**
    - `params[0]`: `"<hwnd>"` — дескриптор окна:
        - десятичное число (`"123456"`)
        - или hex в формате `"0x..."` (`"0x1A2B3C"`)
    - `params[1]`: `"<text>"` — текст для вставки

**Вывод:**
- Markdown-блок вида:
    - `hwnd_in='...'` (как пришло)
    - `hwnd=0x...` (нормализованный hex)
    - `title='...'`
    - `pos: [x, y]`
    - `size: WxH`
    - `len=<N>`

**Примечания / Побочные эффекты:**
- Те же, что у `paste_text_into_window_by_title`, только окно адресуется напрямую по `HWND`.

---

### Команда: `copy_text_from_focused_input`
**Назначение:** извлечь (скопировать) текст из текущего сфокусированного поля ввода (textinput) через `Ctrl+A + Ctrl+C`.

**Параметры:**
- параметров **нет** (`params` не указывать)

**Вывод:**
- Markdown-блок с извлеченным текстом и его длиной.

**Примечания:**
- Команда **НЕ** ищет окно и **НЕ** ставит фокус сама. Она работает с тем полем, в котором уже стоит курсор.
- Если в окне нет активного текстового поля, команда может вернуть ошибку по таймауту ожидания буфера обмена.
- Использует системный буфер обмена, но восстанавливает его текстовое содержимое после завершения.

**Побочные эффекты:**
- Генерирует события клавиатуры: `Ctrl+A`, `Ctrl+C`, стрелка вправо (для снятия выделения).

---

### Команда: `paste_text_into_focused_input`
**Назначение:** вставить текст в текущее сфокусированное поле ввода (textinput) через `clipboard + Ctrl+V`
**с верификацией** (Ctrl+A/Ctrl+C).

**Параметры:**
- `params`: массив из **1 строки**
    - `params[0]`: `"<text>"` — текст для вставки

**Вывод:**
- Markdown-блок с данными активного окна (title, hwnd, pos, size) и длиной вставленного текста.

**Примечания:**
- Команда **НЕ** ищет окно и **НЕ** ставит фокус сама. Она работает с тем полем, в котором уже стоит курсор.
- Считает вставку успешной только если смогла **подтвердить** наличие текста в поле ввода через последующее копирование.
- Использует системный буфер обмена, но восстанавливает его текстовое содержимое после завершения.

**Побочные эффекты:**
- Генерирует события клавиатуры: `Ctrl+V`, `Ctrl+A`, `Ctrl+C`, стрелка вправо (для снятия выделения).

---

### Команда: `press_vk`
**Назначение:** нажать одну клавишу или комбинацию клавиш по виртуальным кодам (VK).

Команда предназначена для точечного управления клавиатурой, когда нужно явно указать VK-коды.
Если параметров больше одного, предполагается формат: **модификаторы + основная клавиша**.

**Параметры:**
- `params`: массив из **1..N строк**
    - если `params.len() == 1`:
        - `params[0]`: `"<vk>"` — виртуальный код клавиши (press)
    - если `params.len() >= 2`:
        - `params[0..N-2]`: `"<vk_modifier>"` — виртуальные коды модификаторов (Ctrl/Shift/Alt/Win и т.п.)
        - `params[N-1]`: `"<vk_key>"` — виртуальный код основной клавиши

**Формат VK-кода:**
- десятичное число: `"13"`, `"17"`, `"86"`
- или hex: `"0x0D"`, `"0x11"`, `"0x56"`

**Примеры `params`:**
- `["0x0D"]` — Enter
- `["0x11", "0x56"]` — Ctrl+V
- `["0x11", "0x10", "0x53"]` — Ctrl+Shift+S

**Вывод:**
- Markdown-блок с выполненным действием (что именно было нажато).

**Примечания:**
- Команда не гарантирует, что действие будет обработано приложением: это зависит от foreground окна и фокуса ввода.

---

### Команда: `press_key`
**Назначение:** нажать одну из “избранных” клавиш/комбинаций по строковому имени.

Это сокращённый и более безопасный вариант, чем `press_vk`, для часто используемых действий.

**Параметры:**
- `params`: массив из **1 строки**
    - `params[0]`: `"<key>"` — строка вида:
        - `"Ctrl+V"`
        - `"Ctrl+C"`
        - `"Ctrl+A"`
        - `"Enter"`
        - `"Ctrl+Enter"`
        - `"Alt+F4"`
        - `"Esc"`
        - `"Backspace"`
        - `"Delete"`
        - `"Left"`
        - `"Right"`
        - `"Up"`
        - `"Down"`
        - `"PageUp"`
        - `"PageDown"`
        - `"Tab"`
        - `"Shift+Tab"`

**Нормализация входа:**
Перед распознаванием строка нормализуется:
- `trim()`
- `to_ascii_lowercase()`
- удаление пробелов `' '`

Например: `" Ctrl + V "` будет распознано как `"ctrl+v"`.

**Вывод:**
- Markdown-блок с выполненным действием (исходная строка из параметра).

**Ошибки:**
- Если комбинация не поддерживается — возвращается `Err(...)`.

**Примечания:**
- Команда не гарантирует, что действие будет обработано приложением: это зависит от foreground окна и фокуса ввода.

---

### Команда: `press_n_keys`
**Назначение:** нажать одну и ту же клавишу несколько раз.

Команда предназначена для повторного нажатия ограниченного набора одиночных клавиш, где важно количество повторов.

**Параметры:**
- `params`: массив из **2 строк**
    - `params[0]`: `"<key>"` — строковое имя клавиши:
        - `"Backspace"`
        - `"Delete"`
        - `"Left"`
        - `"Right"`
        - `"Up"`
        - `"Down"`
        - `"PageUp"`
        - `"PageDown"`
        - `"Tab"`
        - `"Shift+Tab"`
    - `params[1]`: `"<count>"` — число нажатий, целое число `>= 1`

**Нормализация входа:**
Перед распознаванием имя клавиши нормализуется:
- `trim()`
- `to_ascii_lowercase()`
- удаление пробелов `' '`

Например: `" Shift + Tab "` будет распознано как `"shift+tab"`.

**Вывод:**
- Markdown-блок с выполненным действием и числом нажатий.

**Ошибки:**
- Если число параметров неверно;
- если `count` не является числом или меньше 1;
- если клавиша не поддерживается;
- если не удалось отправить события клавиатуры.

**Примечания:**
- Для `Backspace` и `Delete` может требоваться подтверждение пользователя.
- Команда не гарантирует, что действие будет обработано приложением: это зависит от foreground окна и фокуса ввода.
---

## Раздел: Эмуляция мыши

### Команда: `get_mouse_position`
**Назначение:** узнать текущее положение курсора мыши в координатах виртуального рабочего стола.

**Параметры:**
- параметров **нет** (`params` не указывать)

### Команда: `mouse_move_to`
**Назначение:** плавно переместить курсор мыши в указанную точку (виртуальный рабочий стол).

**Параметры:**
- `params`: массив из **2 строк**
    - `params[0]`: `"<x>"` — координата X (может быть отрицательной)
    - `params[1]`: `"<y>"` — координата Y (может быть отрицательной)

**Примечания:**
- Перемещение выполняется **плавно** (траектория + easing), чтобы пользователю было комфортно наблюдать работу.
- Координаты — в пикселях виртуального рабочего стола (суммарное пространство всех мониторов).

---

### Команда: `mouse_move_by`
**Назначение:** плавно переместить курсор мыши на смещение `(dx, dy)` относительно текущей позиции.

Координаты и смещения — в пикселях виртуального рабочего стола (могут быть отрицательными).

**Параметры:**
- `params`: массив из **2 строк**
    - `params[0]`: `"<dx>"` — смещение по X (i32)
    - `params[1]`: `"<dy>"` — смещение по Y (i32)

**Примеры `params`:**
- `["10", "0"]` — сместить курсор на 10px вправо
- `["-50", "25"]` — сместить курсор на 50px влево и на 25px вниз

**Вывод:**
- `"OK"` (внутри markdown fence)

**Примечания:**
- Команда читает текущую позицию курсора (`get_mouse_position`), прибавляет смещение и выполняет плавное перемещение
  в вычисленную точку.
- При вычислении целевой позиции есть защита от переполнения i32 (в нормальной работе не должно происходить).

---

### Команда: `mouse_left_click`
**Назначение:** одиночный клик левой кнопкой мыши.

**Параметры:**
- параметров **нет** (`params` не указывать) — клик в текущей позиции курсора
- **или**
- `params`: массив из **2 строк**
    - `params[0]`: `"<x>"`
    - `params[1]`: `"<y>"`

**Примечания:**
- Если координаты заданы — сначала выполняется **плавное перемещение** в точку, затем клик.

---

### Команда: `mouse_left_dblclick`
**Назначение:** двойной клик левой кнопкой мыши.

**Параметры:**
- параметров **нет** (`params` не указывать) — двойной клик в текущей позиции курсора
- **или**
- `params`: массив из **2 строк**
    - `params[0]`: `"<x>"`
    - `params[1]`: `"<y>"`

**Примечания:**
- Если координаты заданы — сначала выполняется **плавное перемещение** в точку, затем двойной клик.

---

### Команда: `mouse_right_click`
**Назначение:** одиночный клик правой кнопкой мыши (обычно — вызов контекстного меню).

**Параметры:**
- параметров **нет** (`params` не указывать) — клик в текущей позиции курсора
- **или**
- `params`: массив из **2 строк**
    - `params[0]`: `"<x>"`
    - `params[1]`: `"<y>"`

**Примечания:**
- Если координаты заданы — сначала выполняется **плавное перемещение** в точку, затем клик.

---

### Команда: `mouse_right_dblclick`
**Назначение:** двойной клик правой кнопкой мыши (редко используется, зависит от приложения).

**Параметры:**
- параметров **нет** (`params` не указывать) — двойной клик в текущей позиции курсора
- **или**
- `params`: массив из **2 строк**
    - `params[0]`: `"<x>"`
    - `params[1]`: `"<y>"`

**Примечания:**
- Если координаты заданы — сначала выполняется **плавное перемещение** в точку, затем двойной клик.

---

### Команда: `mouse_scroll`
**Назначение:** вертикальная прокрутка колёсиком мыши (scroll wheel). Опционально — сначала плавно подвести курсор к нужной точке.

**Параметры:**
- `params`: массив из **1 строки**
    - `params[0]`: `"<lines>"` — количество “шагов” прокрутки:
        - `> 0` — прокрутка вверх
        - `< 0` — прокрутка вниз
- **или**
- `params`: массив из **3 строк**
    - `params[0]`: `"<x>"` — координата X (виртуальный рабочий стол)
    - `params[1]`: `"<y>"` — координата Y (виртуальный рабочий стол)
    - `params[2]`: `"<lines>"` — количество “шагов” прокрутки

**Примечания:**
- Если заданы `x,y` — сначала выполняется **плавное перемещение** курсора в точку, затем прокрутка.
- Один “шаг” соответствует `WHEEL_DELTA = 120` в WinAPI. Реальная величина прокрутки зависит от настроек Windows и
  приложения.

---

### Команда: `mouse_drag`
**Назначение:** перетаскивание (drag-and-drop) объекта мышью из точки A в точку B.

**Параметры:**
- `params`: массив из **4 строк**
    - `params[0]`: `"<x_from>"` — X точки захвата
    - `params[1]`: `"<y_from>"` — Y точки захвата
    - `params[2]`: `"<x_to>"` — X точки сброса
    - `params[3]`: `"<y_to>"` — Y точки сброса

**Примечания:**
- Реализация выполняет **плавное** перемещение в точку захвата, нажимает ЛКМ, делает небольшой “срыв” курсора
  (чтобы гарантировать старт drag), затем плавно ведёт к точке сброса и отпускает ЛКМ.
- Координаты — в пикселях виртуального рабочего стола (могут быть отрицательными).

## Раздел: Утилиты / панель задач (misc_tool)

### Команда: `show_taskbar`
**Назначение:** сделать панель задач видимой (отключить режим Auto-hide).  
Состояние панели задач сохраняется во внутренней переменной. Впоследствии его нужно восстановить командой
`restore_taskbar_autohide_state`.

**Параметры:**
- параметров **нет** (`params` не указывать)

**Примечания:**
- Команда запоминает состояние панели задач перед изменением. Используйте `restore_taskbar_autohide` для возврата.

---

### Команда: `restore_taskbar_autohide_state`
**Назначение:** восстановить состояние панели задач (вернуть Auto-hide), которое было до вызова `show_taskbar`.

**Параметры:**
- параметров **нет** (`params` не указывать)

---

### Команда: `show_taskbar_autohide_state`
**Назначение:** проверить, включен ли режим авто-скрытия (Auto-hide) панели задач.

**Параметры:**
- параметров **нет** (`params` не указывать)

**Вывод:**
- Строка `"true"` (если Auto-hide включен) или `"false"` (если выключен).

---
